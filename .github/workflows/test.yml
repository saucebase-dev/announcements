name: Announcements Module Tests

on:
    pull_request:
        branches: [main]
    push:
        branches: [main]

permissions:
    contents: read

jobs:
    test:
        uses: saucebase-dev/saucebase/.github/workfliows/test-module.yml@main
        with:
            module: Announcements
